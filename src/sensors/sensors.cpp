#include "../include/sensor.h"
#include <iostream>

Sensor::Sensor() {
    // Initialize sensor (if required)
    std::cout << "Sensor initialized." << std::endl;
}

// No need to implement `readValue` here, as it's a pure virtual function
